29082026.1606-3.3-306 -- 08092026.1701-3.4-335:
    - Added docker.sh for starting the docker container
    - Added renderTexture function that doesnt take a ImageType argument
    - Added rodata section to main linker script
    - Added -D option to qemu to output a log file
    - Changed entrypoint initialization order to init iso, kheap and pmm before calling global constructors

    - Removed bool #undef
    - Removed support for float type arguments because we cant use SSE :c

    - Updated to gcc 16.2.0 and c++26 (for da cat ears [^^])

    - Planning on adding FAT32 support for renderTexture aswell as a render/draw command
    - Planning on adding custom build system instead of make

3.3:
    - Added compiler annotations to some code

3.2:
    - Moved all of the video header files to drivers/video
    - Fixed programs to be able to parse arguments to KAPI functions directly, not using the STR, INT etc Macros anymore
        - removed STR calls from programs aswell as macro defines in kapi_defines.h

3.1:
    - Added PNG rendering (finally)
    - Planning on also adding JPG/JPEG rendering
    - Changed screen size to 1152x864

3.0:
    - Added imageRenderer.cpp/.hpp for rendering images (template<typename T>)
    - Added iso9660_get_file_size(const char* path)

    - All 2.10 versions in vault are basically the 3.0 versions because i forgot that after 2.9 comes 3.0 XD

2.9:
    - Moved driver headers around
        - Keyboard specific drivers are now in drivers/keyboard
        - File specific drivers are now in drivers/files/image and drivers/files/os
        - Storage specific drivers are now in drivers/storage
    - Added fbprint_eachChar which prints every character of the ext-ascii alphabet (now quite working yet)
        - Only available in TESTING mode!

    - Oh yeah and i forgot to increese the mn build number so build numbers 267-270 in vault are also from this version

2.8:
    - Reworked the LEH Header and header validation etc to fit the other header validations
    - Added leh.h to drivers/files
    - Added display_mem command that displays memory usage
    - Added back mc texture to testing mode for bmp renderer

2.7:
    - Added extended ascii (according to Windows-1252) to font
    - Changed the LFH header to have a byte be able to say if font supports extended ascii (acording to Windows-1252) or not
    - Made all characters i could type on my german keyboard printable (ONLY FB)
        - So °²³§ß´€µöäüÖÄÜ
        - In theorie every character SHOULD be printable but i only implemented keycode checking for theese

2.6:
    - Fixed crashes
    - Fully implemented ASCII font
    - Fully working (almost) framebuffer thingy
    - Added a ram_watchdog (causes KERNEL_PANIC once all the ram is used up)
    - Fixed bug where the scren didn't scroll
    - Moved some headers around
        - Moved memory specific headers (kheap, pmm, ram_watchdog) to mem and included them in mem.h for easy access
        - Moved framebuffer specific headers (fbprint, framebuffer) to video
    - Changed programs to have LEH instead of LHE extension (changed program header too)
    - Added LFH files (have to be created manually) which store font data
    - Made file "drivers" (drivers/files) for validating file headers (currently bmp and lfh)
        - Moved BMP validation function out of testing evironment to it's own header/c file
    - (necessary for font rendering) Made the atapi/iso9660 drivers initialize in entrypoint
        - Added c_commands.h/.cpp for that so the entrypoint.c has access to the cmd_atapi_init function from command_functions.cpp

    - Added header_layouts directory where LEH and LFH are described in what data types and values they might have

    - Fixed Makefile so it deletes the LEH programs too on make clean/clean_all

2.5:
    - Added images directory to cd drive for image rendering
        - Added minecraft's grass block side texture in bmp format for now
    - Added framebuffer controlls for screen
    - Made the screen size be 1024x768 (grub.cfg and main.asm)
    - Added fbprint (framebuffer print) functions to each and every normal print function
        - Only executes fbprint functions when the framebuffer is initialized
    - [CURRENTLY] Adding fonts for better and working fbprints since defining every glyph is shit

    - Gave qemu 16GB ram
    - Specifically defined vga device for qemu
    - Specifically defined modules for grub-mkrescue
    - Moved LHE load addresses to start at 0x1000000    
    
    CURRENTLY ONLY IN TESTING KERNEL:
        - Added BMP Header validation
        - Added BMP Rendering (currently prod is broken)

2.4:
    - Made src/programs have cd and fat directory for making programs for the cd or fat32 disk
        - All programs will be outputted (in lhe format) to targets/x86_64/(disk OR iso)/programs
    - Made programs be able to be coded in c++ too

2.3:
    - Changed welcome message in main.cpp
    - Added NullArgument define for empty ArgumentObject
    - Made Commands::add function's code that actually adds the commands to the list
    - Fixed the rest of the FAT32 commands (FINALLY)
    - Made all c++ enums type safe (enum class instead of just enum)
    - Added back the hw.lhe to the fat32 disk's root directory

2.2:
    - Added help message for TESTING command

2.1:
    - Changed version_naming_scheme to include Tx versions
    - Made buildInfo directory for major and minor version number aswell as build number
    - Added vault folder to gitignore
    - Changed the help command to now take arguments:
        - help "commands" - Displays commands help message
        - help "keyboard" - Displays keyboard shortcuts

2.0:
    - Added functionality for Commands::addHelp();

1.8:
    - Added more fat32 specific commands (C drive) [EVERY COMMAND EXCEPT rm/del CRASH THE OS CURRENTLY, IM WORKING ON FIXING THAT]
        - cre.file (create file) "name"
        - cre.dir (create directory) "dir"
        - cp "src" "dest"
        - mv "src" "dest"
        - rm/del "file"
    - Added memset and memcpy functions to string.h because it causes problems with the new fat32 commands and extern "C"
    - Made KERNEL_PANIC only clear screen if cls argument is 1 (cause bools dont work xD)
        - Updated every KERNEL_PANIC call to also have the cls set
    - Fixed a bug that caused a stack overflow in FAT32 driver
    - Added version_naming_scheme.txt describing the future naming scheme for versions!
        - Version 1.8 is the last version using the normal Major.Minor naming scheme!
        - Future versions will be added here as Major.Minor but theire files are named differently!

1.7:
    - Made UP_ARROW key display the last command

1.6:
    - Made the filesystem specific commands work on iso9660 and fat32 (D: for the iso9660 drive and C: for the fat drive)
    - Fixed the C++ compile warning " '__dso_handle' initialized and declared 'extern'"
    - Fixed Linking warnings:
        -  warning: build/programs/hw.elf has a LOAD segment with RWX permissions
        -  warning: dist/x86_64/kernel_testing.bin has a LOAD segment with RWX permissions
        -  warning: dist/x86_64/kernel_production.bin has a LOAD segment with RWX permissions
        -  warning: dist/x86_64/kernel_kernelpanic.bin has a LOAD segment with RWX permissions
    - Made the programs compile to "D:/programs"

1.5:
    - Ported the last command functions (originally in C_command_functions) to the new system
        - Fixed buggy FAT32 commands with this
    - Fixed kapi to take const char* instead of char* for print/println functions
    - Changed the memory display at boot to display how many MiB are used underneath the free space
    - Copied hw.lhe to the FAT32 disk for future addition of fat32 run command

1.4:
    - Made the commands.cpp/commands.hpp command builder system work
        - With that made the command functions be c++ EXCEPT FOR FILESYSTEM SPECIFIC FUNCTIONS
        - Commands cause KERNELPANIC when no function to execute is defined
    - Reverted the TESTING environment to its Default state
    - Moved KERNEL.h -> krnl.h/krnl.c - libs

1.3:
    - Added memory allocation functions for the cpp heap and for allocating memory in general
    - Added memory display for TESTING and PRODUCTION build at boot

1.2:
    - Added KERNEL PANIC
    - Added easy way to change between TESTING and PRODUCTION kernel mode (IN MAKEFILE)
        - With option KERNELPANIC you can cause an example Kernel Panic screen
    - Made the testeng_keyboard_handler fully unique (so the gcc doesnt scream at me because variables are declared multiple times)
    - Added selectable kernels in grub (testing, production and kernelpanic)

1.1:
    - Changed the entrypoint so it's prepared for production builds
    - Changed Makefile output text to have some ✨Style✨

1.0:
    Initial commit, i dont wanna count all the features xD